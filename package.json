{
  "name": "emdp-lab-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node tools/site/build.mjs",
    "check": "node --test tests/*.test.mjs",
    "check:site": "node tools/site/check.mjs",
    "test": "npm run check && npm run check:site"
  }
}
